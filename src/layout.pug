doctype html
html
  head
    meta(charset='utf-8')
    title Local microblogging client
    block head
  body.body
    .container
      ul.news-list
        li.news-list__item.news-list__item_first
          h3.h3 What's new?
          form(role='form')
            .form-group
              input.form-control.new-post__heading(placeholder='Your heading', type='text')
            .form-group
              textarea.form-control.new-post__main-text(placeholder='Write something', rows='3')
            button.btn.btn-default.new-post__add-image(title='Add image')
              i.glyphicon.glyphicon-picture
            button.btn.btn-primary.new-post__submit(title='Add new post', type='button', onclick='onSubmit()')
              i.glyphicon.glyphicon-ok
              | Post it
      block scripts
